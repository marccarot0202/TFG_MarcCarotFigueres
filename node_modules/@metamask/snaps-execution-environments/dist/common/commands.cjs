"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCommandMethodImplementations = exports.getHandlerArguments = void 0;
const snaps_utils_1 = require("@metamask/snaps-utils");
const utils_1 = require("@metamask/utils");
const validation_1 = require("./validation.cjs");
/**
 * Formats the arguments for the given handler.
 *
 * @param origin - The origin of the request.
 * @param handler - The handler to pass the request to.
 * @param request - The request object.
 * @returns The formatted arguments.
 */
function getHandlerArguments(origin, handler, request) {
    // `request` is already validated by the time this function is called.
    switch (handler) {
        case snaps_utils_1.HandlerType.OnTransaction: {
            (0, validation_1.assertIsOnTransactionRequestArguments)(request.params);
            const { transaction, chainId, transactionOrigin } = request.params;
            return {
                transaction,
                chainId,
                transactionOrigin,
            };
        }
        case snaps_utils_1.HandlerType.OnSignature: {
            (0, validation_1.assertIsOnSignatureRequestArguments)(request.params);
            const { signature, signatureOrigin } = request.params;
            return { signature, signatureOrigin };
        }
        case snaps_utils_1.HandlerType.OnAssetHistoricalPrice: {
            (0, validation_1.assertIsOnAssetHistoricalPriceRequestArguments)(request.params);
            const { from, to } = request.params;
            return { from, to };
        }
        case snaps_utils_1.HandlerType.OnAssetsLookup: {
            (0, validation_1.assertIsOnAssetsLookupRequestArguments)(request.params);
            const { assets } = request.params;
            return { assets };
        }
        case snaps_utils_1.HandlerType.OnAssetsConversion: {
            (0, validation_1.assertIsOnAssetsConversionRequestArguments)(request.params);
            const { conversions } = request.params;
            return { conversions };
        }
        case snaps_utils_1.HandlerType.OnAssetsMarketData: {
            (0, validation_1.assertIsOnAssetsMarketDataRequestArguments)(request.params);
            const { assets } = request.params;
            return { assets };
        }
        case snaps_utils_1.HandlerType.OnNameLookup: {
            (0, validation_1.assertIsOnNameLookupRequestArguments)(request.params);
            // TS complains that domain/address are not part of the type
            // casting here as we've already validated the request args in the above step.
            const { chainId, domain, address } = request.params;
            return domain
                ? {
                    chainId,
                    domain,
                }
                : {
                    chainId,
                    address,
                };
        }
        case snaps_utils_1.HandlerType.OnProtocolRequest: {
            (0, validation_1.assertIsOnProtocolRequestArguments)(request.params);
            const { request: nestedRequest, scope } = request.params;
            return { origin, request: nestedRequest, scope };
        }
        case snaps_utils_1.HandlerType.OnWebSocketEvent: {
            (0, validation_1.assertIsOnWebSocketEventArguments)(request.params);
            const { event } = request.params;
            return { event };
        }
        case snaps_utils_1.HandlerType.OnRpcRequest:
        case snaps_utils_1.HandlerType.OnKeyringRequest:
            return { origin, request };
        case snaps_utils_1.HandlerType.OnClientRequest:
        case snaps_utils_1.HandlerType.OnCronjob:
            return { request };
        case snaps_utils_1.HandlerType.OnInstall:
        case snaps_utils_1.HandlerType.OnUpdate:
        case snaps_utils_1.HandlerType.OnStart:
        case snaps_utils_1.HandlerType.OnActive:
        case snaps_utils_1.HandlerType.OnInactive:
            return { origin };
        case snaps_utils_1.HandlerType.OnHomePage:
        case snaps_utils_1.HandlerType.OnSettingsPage:
            return {};
        case snaps_utils_1.HandlerType.OnUserInput: {
            (0, validation_1.assertIsOnUserInputRequestArguments)(request.params);
            const { id, event, context } = request.params;
            return { id, event, context };
        }
        default:
            return (0, utils_1.assertExhaustive)(handler);
    }
}
exports.getHandlerArguments = getHandlerArguments;
/**
 * Gets an object mapping internal, "command" JSON-RPC method names to their
 * implementations.
 *
 * @param startSnap - A function that starts a snap.
 * @param invokeSnap - A function that invokes the RPC method handler of a
 * snap.
 * @param onTerminate - A function that will be called when this executor is
 * terminated in order to handle cleanup tasks.
 * @returns An object containing the "command" method implementations.
 */
function getCommandMethodImplementations(startSnap, invokeSnap, onTerminate) {
    return {
        ping: async () => Promise.resolve('OK'),
        terminate: async () => {
            onTerminate();
            return Promise.resolve('OK');
        },
        executeSnap: async (snapId, sourceCode, endowments) => {
            await startSnap(snapId, sourceCode, endowments);
            return 'OK';
        },
        snapRpc: async (target, handler, origin, request) => {
            return ((await invokeSnap(target, handler, getHandlerArguments(origin, handler, request))) ?? null);
        },
    };
}
exports.getCommandMethodImplementations = getCommandMethodImplementations;
//# sourceMappingURL=commands.cjs.map