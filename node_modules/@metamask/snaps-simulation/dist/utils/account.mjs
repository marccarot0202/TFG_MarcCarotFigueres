export const addSnapMetadataToAccount = (account, snapId) => {
    if (!account.owned) {
        return {
            ...account,
            metadata: {},
        };
    }
    return {
        ...account,
        metadata: {
            snap: {
                id: snapId,
            },
        },
    };
};
//# sourceMappingURL=account.mjs.map