import $reduxjstoolkit from "@reduxjs/toolkit";
const { createSlice } = $reduxjstoolkit;
/**
 * The initial chain state.
 */
const INITIAL_STATE = {
    chainId: '0x1',
};
export const chainSlice = createSlice({
    name: 'chain',
    initialState: INITIAL_STATE,
    reducers: {
        setChain: (state, action) => {
            state.chainId = action.payload;
        },
    },
});
export const { setChain } = chainSlice.actions;
/**
 * Get the chain ID from the state.
 *
 * @param state - The application state.
 * @returns The chain ID.
 */
export const getChainId = (state) => state.chain.chainId;
//# sourceMappingURL=chain.mjs.map